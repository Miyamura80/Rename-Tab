{
    "name": "rename-tab-extension",
    "private": true,
    "version": "0.1.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "typecheck": "tsc --noEmit",
        "zip": "vite build && cd dist && zip -r -X ../rename-tab.zip . && cd .."
    },
    "devDependencies": {
        "@crxjs/vite-plugin": "^2.4.0",
        "@types/chrome": "^0.1.42",
        "typescript": "^5.7.0",
        "vite": "^7.3.1"
    }
}
